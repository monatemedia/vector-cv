# Auth Credentials
AUTH_USERNAME=user
AUTH_PASSWORD=password  # This can be plain text initially, we will hash it below
AUTH_NAME="Admin User"
AUTH_EMAIL="admin@example.com"

# Cookie settings for persistence
COOKIE_NAME="resume_synthesizer_auth"
COOKIE_KEY="some_very_random_secret_string"

# Database Components
DB_USER=resume_user
DB_PASSWORD=resume_pass
DB_HOST=localhost
DB_PORT=5433
DB_NAME=resume_db

# Hardcoded DATABASE_URL for compatibility
# DATABASE_URL=postgresql://resume_user:resume_pass@localhost:5432/resume_db

# OpenAI API
OPENAI_API_KEY=your_openai_key_here